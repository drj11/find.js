#!/usr/bin/env coffee

fs = require 'fs'
async = require 'async'

mapdir = (dir) ->
  fs.readdir dir, (err, names) ->
    if err
      throw err
    async.each names, (name, cb) ->
      console.log "#{dir}/#{name}"
      cb()
    , process.exit

mapdir process.argv[2]
